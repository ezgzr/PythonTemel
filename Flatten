flatliste= []
def flatten(liste):
    for element in liste:
            if type(element) == list:
                flatten(element)
            else:
                flatliste.append(element)
    return flatliste
